You are a TypeScript and React developer working with ArcGIS Experience Builder.

Task:
Update the method #sym:onBuscar to conditionally execute an additional query when the selected category is "Turismo".

Specifications:
- After executing the main query:
  - If the query corresponds to "Turismo", perform an additional request to the following service:
    https://sigquindio.gov.co/arcgis/rest/services/QUINDIO_III/TurismoAlfanumerico/MapServer/0/query

- Use the following parameters for the request:
  - f: json
  - where: OBJECTID = <value>
  - returnGeometry: false
  - spatialRel: esriSpatialRelIntersects
  - outFields: TIPO_SERVICIO
  - outSR: 3115

- The OBJECTID value must be obtained from the attributes of the first query response (features[0].attributes).
- If OBJECTID is null or undefined, do not execute the additional query.

Data handling:
- Process the response of the second query and transform it into a structure compatible with #sym:PanelInformativo.
- Map the result into the property #sym:chipsIconoTextoItems.

Constraints:
- Do not break the existing logic of #sym:onBuscar.
- Avoid duplicate or unnecessary requests.
- Handle asynchronous operations properly (Promises / async-await).
- Use strict TypeScript typing for responses.
- Use JSDoc to document all added or modified code.

Output:
Return only the updated code.

///////////////////////////////////////
You are a TypeScript and React developer working with ArcGIS Experience Builder.

Task:
Modify #sym:onBuscar to transform and prepare the response data to be displayed in #sym:PanelInformativo through the property #sym:chipsTextoItems.

Specifications:
- Extract the following attributes from #sym:features:
  - NUMEROCAMAS
  - NUMEROHABITACIONES
  - SALONESEVENTOSCONFERENCIAS

- Example values from the service response:
  - NUMEROCAMAS: "138"
  - NUMEROHABITACIONES: 49
  - SALONESEVENTOSCONFERENCIAS: 0

- Transform these attributes into the structure required by #sym:chipsTextoItems.
- Ensure the displayed values are clear and user-friendly.
- Handle null, undefined, or empty values safely.
- Do not break the existing logic inside #sym:onBuscar.
- Keep the implementation clean, reusable, and maintainable.
- Use strict TypeScript typing for all transformed data structures.
- Use JSDoc to document all new or modified code.

Constraints:
- Avoid unnecessary re-renders.
- Follow React and Experience Builder best practices.

Output:
Return only the updated code.

////////////////////////////////////////
The image needs to resize in width and height as its container changes; currently, the image remains static in terms of its dimensions regardless of whether the container changes. Or better yet, make it so that when the user clicks on the image, it enlarges by 20%.
**********************************************

- si el feature.attributes.IMAGEN tiene información valida del path de una imagen, ejemplo "feature.attributes.IMAGEN
'SIG_QUINDIO/IMAGENES/INDUSTRIA Y COMERCIO/CIRCASIA/IMG_0071.JPG'"
enviarlo al widget con id = "widget_103" por medio del metodo #sym:sendDataExternalWidget .
- ocultar los campos del formulario que actualmente se muestran en el front, para renderizar la IMG que llega en data.IMG
- por medio de un boton se debe poder volver a retornar al formulario ocultando la IMG renderizada

- if feature.attributes.IMAGEN contains valid information about an image path, for example "feature.attributes.IMAGEN
'SIG_QUINDIO/IMAGES/INDUSTRY AND COMMERCE/CIRCASIA/IMG_0071.JPG'"
send it to the widget with id = “widget_103” using the #sym:sendDataExternalWidget method.
- hide the form fields currently displayed on the front end to render the image received in data.IMG
- A button should allow you to return to the form while hiding the rendered IMG

